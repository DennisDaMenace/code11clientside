'use strict';

// $('#submitNewButton', '#submitUpdateButton').on('click', () => $.post('/PLACEHOLDER-URL(NEED TO CHANGE)', { authors: this.authors, title: this.title }))
//   .then(data => {
//     console.log(data);
//     if (callback) callback();
//   });